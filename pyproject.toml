[tool.poetry]
name = "cyberark-to-op"
version = "0.1.0"
description = ""
authors = ["Rob Marshall <rob@robmarshall.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.32.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
